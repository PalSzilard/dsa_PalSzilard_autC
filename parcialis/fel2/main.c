#include "function.h"
#include <stdio.h>
#include <stdlib.h>

int main() {
    N_Type* first=NULL;
    read(first);
    printf("az elso 19el oszthato szam gyoke: %lf\n",firstsqrt(first));
    printf("a szamok atlaganak a negyzete: %lf\n",avg(first));
    printf("%i",bigerten(first));

    return 0;
}
